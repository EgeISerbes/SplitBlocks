using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MidBlock : MonoBehaviour
{

    internal bool isHit = false; // Since there are two triggers, we wouldn't want to build more than 1 stairs so i have used this bool value 
    // Start is called before the first frame update
    

    // Update is called once per frame
    
}
